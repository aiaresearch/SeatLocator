ssh 47.120.18.45 -l HZX

https://bc4n9iha.mirror.aliyuncs.com

sudo docker run --name postgres -e POSTGRES_PASSWORD=Xx080814 -e POSTGRES_USER=postgres -p 5432:5432 -d postgres:latest

sudo docker run --name app2.0 -e PASSWORD=Xx080814 -p 80:5000 a626fd41f696

scp -r server HZX@47.120.18.45:~

sudo docker exec -it postgres /bin/bash
su postgres
psql -d seatdata
CREATE DATABASE seatdata;
CREATE TABLE querynumber_1 (
    id SERIAL PRIMARY KEY,
    occupied_column FLOAT NOT NULL,
    available_column FLOAT NOT NULL,
    timestamp_column TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE querynumber_2 (
    id SERIAL PRIMARY KEY,
    occupied_column FLOAT NOT NULL,
    available_column FLOAT NOT NULL,
    timestamp_column TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);


sudo curl -L "https://github.com/docker/compose/releases/download/v2.2.2/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
